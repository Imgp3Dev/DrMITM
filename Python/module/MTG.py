#MTG = Message Traffic Grabber.
# > Getting Data that was sent to the server
import socket
import httplib
def resend(url):
s = socket.socket()
data = HTTPConnection.set_debuglevel(3)
socket.create_connection(url,[10[url, 80]])
r.send(data)


